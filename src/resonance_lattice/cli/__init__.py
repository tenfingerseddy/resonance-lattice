"""CLI surface — `rlat <command>`.

v2.0 commands (15 SHIP):
  build, optimise, install-encoder, search, profile, compare, summary,
  init-project, refresh, sync, freshness, memory *, skill-context,
  convert.

`serve` was dropped (no current consumer — MCP is the planned bridge
for a future version).

v0.11 commands DELETED: ask, resonate, merge, mcp, query, ingest, add,
ls, info, diff, encoders, export, probe, topology, xray, negotiate,
contradictions (top-level), forget, primer (top-level), locate (top-level),
compose (top-level), all skill * subcommands, all lens * subcommands.
See docs/internal/audits/01_feature_audit.md.
"""
