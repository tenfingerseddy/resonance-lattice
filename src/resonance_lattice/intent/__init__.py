"""Intent Lattice — kind tag only in v2.0.

`rlat build --kind intent ./source -o intent.rlat` produces a knowledge model
with metadata.kind = "intent". Otherwise identical to a corpus knowledge model.

Operators (anti, --toward, multi-intent composition) are deferred to v2.1+.

Phase 3 deliverable. Base plan §9.
"""
