# Harness fixtures (large, opt-in)

BEIR subsets and other corpora >5K passages. Skipped by default in the
per-commit harness selection — these run on `--phase-gate` and nightly.

Populated as benchmark gate work lands (Phase 1 sets the floor).
