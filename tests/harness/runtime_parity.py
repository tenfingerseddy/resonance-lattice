"""Runtime parity — ONNX vs OpenVINO vs PyTorch produce numerically equivalent embeddings.

Tolerance: cosine similarity ≥ 0.9999 between any two runtimes for the same input.

Phase 1 fills.
"""


def run() -> int:
    return 0
