# Workspace-level Library Management is the recommended install path
# (Fabric portal → User Data Functions item → Library Management). This
# file is the project-local fallback for `func host start` emulator runs
# and for tenants that prefer requirements.txt over Library Management.
#
# `fabric-user-data-functions` and `azure-functions` are pre-installed in
# the Fabric runtime; pinning here is for the local emulator only.
#
# No pyarrow: the slicer (`slice_stream`) now streams the band straight out of
# the `.rlat` (store.streaming) — single file, no parquet sidecar — so the UDF
# has no parquet dependency. `[ann]` (faiss-cpu) is only needed by the bundled
# `search`/`slice` path for SMALL corpora; the streaming slicer needs neither.
rlat[fabric,ann]>=2.1.0a20
